import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        borderTopWidth: 1,
        borderColor: "#333333",
        gap: 16,
        paddingVertical: 48,
        alignItems: "center"
    },
    text: {
        color: "#808080"
    }
})